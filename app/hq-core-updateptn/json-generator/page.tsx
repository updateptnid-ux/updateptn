import JsonGeneratorClient from './json-generator-client';

export default function JsonGeneratorPage() {
  // Authentication already handled by parent layout (hq-core-updateptn/layout.tsx)
  return <JsonGeneratorClient />;
}
